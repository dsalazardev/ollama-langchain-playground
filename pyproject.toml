[project]
name = "ollama-langchain-playground"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "langchain-core>=1.0,<2.0",
    "langchain>=1.0,<2.0",
    "langgraph>=1.0,<2.0",
    "langchain-ollama>=0.2",
    "langsmith>=0.3.0",
]
